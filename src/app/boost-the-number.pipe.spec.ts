import { BoostTheNumberPipe } from './boost-the-number.pipe';

describe('BoostTheNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new BoostTheNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
